# 隐私政策与数据收集说明

## 匿名使用情况统计 (Census)

idcops 包含一项可选的匿名使用情况统计功能，帮助开发者了解软件的部署数量和版本分布。

### 收集的数据

该功能**仅**收集以下匿名数据：

| 数据项 | 说明 | 示例 |
|--------|------|------|
| `version` | 软件版本号 | `1.0.0` |
| `python_version` | Python 运行时版本 | `3.11.5` |
| `deployment_id` | 基于 SECRET_KEY 的匿名哈希ID（16位） | `a1b2c3d4e5f67890` |

### 不收集的数据

我们**不会**收集以下信息：

- ❌ IP 地址
- ❌ 用户名、密码或任何用户数据
- ❌ 服务器主机名或域名
- ❌ 数据库内容或业务数据
- ❌ 任何可识别个人身份的信息 (PII)

### 数据用途

收集的匿名数据仅用于：

1. 估算活跃部署数量
2. 追踪版本采用情况
3. 指导开发优先级

### 如何关闭

您可以通过以下任一方式关闭此功能：

**方式 1：设置环境变量**

```bash
# 在 .env 文件中添加
CENSUS_REPORTING_ENABLED=0
```

**方式 2：启用离线部署模式**

```bash
# 在 .env 文件中添加（会跳过所有外部网络请求）
ISOLATED_DEPLOYMENT=1
```

### 上报频率

- 使用情况统计每日最多上报一次
- 上报请求设有 5 秒超时，不会影响应用性能
- 上报失败不会产生任何错误或中断服务

### 透明性

此功能的完整源代码位于：

- 配置：`idcops/settings.py`（搜索 `CENSUS`）
- 实现：`dcrm/tasks/census.py`

您可以随时审查这些代码以验证我们的数据收集范围。

---

## 联系我们

如有任何隐私相关问题，请通过以下方式联系：

- GitHub Issues: [项目地址]
- Email: [联系邮箱]
